package Generator.View;

import javax.swing.*;

public class LabelTextPanel extends JPanel {
    // literally copied from the ca lab lol
    LabelTextPanel(JLabel label, JTextField textField) {
        this.add(label);
        this.add(textField);
    }
}
